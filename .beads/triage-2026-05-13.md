# Audit-bead triage — 2026-05-13

Output of `/goal triage`. 165 child beads across 14 epics filed during the 15-wave session.

**Tags:** `K` = keep, `C` = close (duplicate or doctrinally rejected per CLAUDE.md), `D` = defer (plausible but no current consumer pressure).

**Counts:** Keep 96, Close 7 (dups), Defer 62.

---

## Closes (duplicates and stale wording)

| Bead | Reason |
|---|---|
| C  m80-wok08.1 | DUP of m80-243wj.1 — same TAP/iptables cleanup gap, filed twice across waves |
| C  m80-wok08.8 | DUP of m80-mrjqs.4 — FC PUT /logger capture, filed twice |
| C  m80-mrjqs.2 | DUP of m80-243wj.2 — console.log on GuestdReadyTimeout |
| C  m80-243wj.17 | DUP of m80-r4308.3 — WarmPool::Drop hang on fill-panic |
| C  m80-ij49d.2 | DUP of m80-9ihnr.4 — Limits::m80_default() → preset() README drift |
| C  m80-1vna7.10 | DUP of m80-73qb3.13 — ip_forward sysctl not restored |
| C  m80-1vna7.5 | DUP of m80-r4308.10 + m80-r4308.12 — WarmPool tunables not exposed |

---

## Defers — speculative or doctrinally adjacent to "no defensive ceremony"

Grouped for brevity. Each is plausible; none has a current consumer signal.

**Defensive-ceremony patterns CLAUDE.md explicitly rejects** (we own the callers, internal data is trusted):
- `m80-9ihnr.6` env-var empty-string accepted — defensive on internal env
- `m80-9ihnr.7` SnapshotPaths construction validation — closed call graph
- `m80-9ihnr.10` Scratch upper bound — explicit adapter-boundary per CLAUDE.md
- `m80-r4308.5` owner accept loop read-timeout — local UDS, single tenant
- `m80-r4308.6` owner request size cap — same
- `m80-r4308.10` target_ready upper bound — speculative limit
- `m80-r4308.11` owner startup silent window — operational UX, no bug
- `m80-r4308.12` drain timeout configurable — no consumer wants longer
- `m80-243wj.15` scratch loop CLONE_NEWNS — speculative SIGKILL race in image-build
- `m80-243wj.16` stdio_log CLOEXEC narrow window — closed call graph
- `m80-243wj.18` guestd netlink listener handle discarded — speculative
- `m80-243wj.20` InputThread + owner.sock CLOEXEC — speculative

**No current consumer pain** (torpor2 hasn't hit these):
- `m80-243wj.7` + `m80-243wj.12` reboot survival of run_root
- `m80-ag33f.11` WarmPool::Drop tokio block — torpor2 sync-from-async question
- `m80-ezs0x.6` flatten m80-cgroup &MaterializedJail — works fine
- `m80-ij49d.8` CLAUDE.md/AGENTS.md split — process question
- `m80-ij49d.10` anyhow in guestd — needs human judgment, not a sweep
- `m80-1vna7.6` JailerConfig new_cgroup_ns: false — speculative defense-in-depth

**Compatibility hardening — m80 is x86_64 Linux on operator-controlled host today; defer until torpor2 deploys elsewhere:**
- `m80-73qb3.1` min host kernel floor docs (partial overlap m80-2ggw.1)
- `m80-73qb3.2` ARM cpuinfo misleading error — m80 is x86_64-only by design; one-line doc fix sufficient, drop the larger investigation
- `m80-73qb3.4` CPU microcode / vulnerabilities sysfs — partial overlap m80-2ggw.1
- `m80-73qb3.5` DNS stub 127.0.0.53 edge case
- `m80-73qb3.6` AppArmor/seccomp in container
- `m80-73qb3.7` cgroup v2 delegation in container
- `m80-73qb3.8` systemd service unit — speculative until `m80 warm enable --system` lands
- `m80-73qb3.9` noexec on run_root
- `m80-73qb3.10` fallocate FS-type
- `m80-73qb3.11` iptables backend mismatch

**Snapshot hardening — capture/restore has one consumer (warm-pool single-process); defer the integrity scaffolding until production reuse exists:**
- `m80-kv4ri.1` capture atomicity (gated by .2)
- `m80-kv4ri.2` promote SnapshotManifest out of #[cfg(test)] — load-bearing but only when consumed
- `m80-kv4ri.4` post-capture chown back
- `m80-kv4ri.5` snapshot bind MS_NODEV remount — kernel-touching, smoke-required per CLAUDE.md
- `m80-kv4ri.8` warm-pool shared snapshot RO remount — same, sweep-ineligible
- `m80-kv4ri.10` snapshot rotation split-brain — no consumer rotates
- `m80-kv4ri.12` mem.snap sparse — perf, route through m80-jp6ik
- `m80-kv4ri.15` bind_snapshot_parent canonicalize — closed call graph
- `m80-kv4ri.16` trust model + HMAC — speculative

**Supply-chain hardening on image-build — operator-controlled host, low blast radius:**
- `m80-uh6ex.1` apt packages not pinned
- `m80-uh6ex.2` kernel tag not commit-SHA-pinned + GPG
- `m80-uh6ex.6` rootfs bit-flip silent (long-pool concern only)
- `m80-uh6ex.10` implicit loop device cleanup — private namespace handles it

**Other:**
- `m80-vpifh.6` no fuzz harness — wire is well-hardened per W14 audit; defer until a real bug surfaces

---

## Keepers — grouped into bundles, ranked by leverage

### Tier 1 — operator-blocking bugs (ship first)

#### Bundle A — Network cleanup correctness  [P1, ~1 PR]
TAP devices and iptables rules leak today on any crashed VM. `m80 cleanup` silently makes it worse by destroying the state file before teardown runs.

- m80-243wj.1  recover_stale_run_root deletes network-state.json before cleanup_vm (CRITICAL)
- m80-wok08.2  no CLI for dangling iptables after run-dir gone
- m80-wok08.5  add `m80 doctor` / `m80 net cleanup` commands
- m80-243wj.9  NETWORK_ALLOCATION_MUTEX scope on teardown
- m80-243wj.11 xtables -W + boot_identity error + recover docs (partial)

**Fix-once shape:** call `cleanup_vm` before `remove_dir_all` (or read network-state into memory first); add `m80 net cleanup` that scans iptables by m80-tag and TAPs by `tfc*` prefix; `m80 doctor` as a read-only superset.

#### Bundle B — Warm owner reliability  [P1, ~1 PR]
SIGTERM corrupts the pool. Operator can't see failure state. Fill thread panics are invisible.

- m80-r4308.1  warm owner has no signal handler (orphans owner.sock + identity)
- m80-r4308.2  WarmControlRequest::Status hardcodes last_error=None
- m80-r4308.4  failed_fill_count missing from WarmPoolSnapshot
- m80-r4308.3  WarmPool::Drop hangs forever if fill thread panics post-disarm
- m80-wok08.3  idle watcher panic silently swallowed by `let _ = handle.join()`
- m80-r4308.7  client crash during owner response → owner exits (pool dies)
- m80-mrjqs.3  promote metrics out of _test_internal + CLI metrics + warm depth gauges
- m80-mrjqs.7  force_kill std::mem::forget → tracing::error! before leak
- m80-kv4ri.11 orphan-snapshot GC on warm-owner crash (depends on signal handler)
- m80-wok08.6  call recover_stale_run_root on startup of run + warm owner

**Fix-once shape:** install signal_hook (pty.rs pattern) → route to graceful shutdown → existing cleanup runs; plumb `last_fill_error` + counters through `WarmPoolSnapshot` → Status RPC; wrap fill worker + idle watcher in `catch_unwind` with structured logging; per-conn errors in accept loop don't propagate out.

#### Bundle C — Failure post-mortem visibility  [P1, ~1 PR]
Every guestd failure is opaque today; partial diagnostics get deleted by the launch-failure cleanup guard.

- m80-mrjqs.1  failure_summary.json on every error path + auto-preserve
- m80-mrjqs.10 .preserved/ retention + WarmPool failure preserves slot diagnostics
- m80-243wj.2  console.log not surfaced on GuestdReadyTimeout
- m80-243wj.3  warm-pool slot health check + structured DisconnectBeforeTerminal cause
- m80-mrjqs.4  FC PUT /logger capture (vmm-internal log)
- m80-mrjqs.5  guestd panic hook backtrace + M80_GUEST_PANIC marker + non-PID-1 coverage
- m80-wok08.7  M80_PHASE_TRACE phase events into diagnostics.jsonl

**Fix-once shape:** replace `LaunchRunDirCleanupGuard::drop`'s unconditional delete with write-failure-summary-then-preserve; PUT /logger in phase_11; console.log tail in every guestd-failure error message; phase events durable in diagnostics.jsonl regardless of M80_PHASE_TRACE.

### Tier 2 — Consumer-API and governance (do alongside Tier 1 work)

#### Bundle D — FcError surface hygiene  [P1, ~1 PR]
Direct torpor2 consumer ergonomics. CLAUDE.md doctrine: "Errors carry finite typed variants plus detail, not Other(String)/Generic with prose."

- m80-ag33f.1  README — preflight precondition + Arc<Backend> + AdmissionRefused + compile-tested example
- m80-ag33f.2  FcError needs kind()/is_retryable() — consumer recovery decisions impossible
- m80-ag33f.3  eliminate FcError::Io anonymous catch-all
- m80-mrjqs.8  FcError::Io → PathIo at 7 raise sites
- m80-mrjqs.9  FcError::hint() method (parity with PreflightError)
- m80-wok08.9  WarmPoolFillFailed { detail: String } → structured Arc<FcError>
- m80-mrjqs.13 exit-code regrouping + exhaustiveness test for every FcError variant
- m80-kv4ri.14 + m80-wok08.12 — parse FC fault strings into typed ClientError variants

**Fix-once shape:** retire `Io(#[from] io::Error)`; introduce `PathIo`/`CommandSpawnFailed`/`Network`/`Storage` peers; add `kind() -> ErrorKind` + `is_retryable() -> bool` + `hint() -> Option<&str>`; exhaustiveness test pins every variant.

#### Bundle E — README + behavior-doc drift batch  [P1+P2, ~1 PR, 1 day]
Highest closeout-per-hour ratio in the whole corpus. Pure docs.

- m80-ij49d.1  m80-firecracker README (pub vs pub(crate))
- m80-ij49d.3  m80-cli README (non-existent flags)
- m80-ij49d.4  m80-image-build + manifest READMEs
- m80-ij49d.5  m80-storage README (SwapFailed variant claim wrong)
- m80-ij49d.6  m80-jailer + snapshot + vsock READMEs
- m80-ij49d.7  docs/behaviors/* path drift
- m80-ij49d.9  docs/adapter-boundary.md "future consumer" stale (torpor2 is live)
- m80-9ihnr.4  Limits::m80_default() → preset() (in cgroup + jailer READMEs)
- m80-ag33f.10 adapter-boundary trust assumptions — 6 undocumented invariants
- m80-ezs0x.5  floating version-language in 5 comments
- m80-1vna7.7  M80_JAIL_UID + M80_JAIL_GID undocumented
- m80-kv4ri.6  document clock semantics on restore (CLOCK_MONOTONIC/REALTIME)
- m80-kv4ri.7  document capture page-cache safety contract
- m80-kv4ri.9  document RESTORED_SLOT_SETTLE (+ expose as WarmPoolConfig field)
- m80-vpifh.4  document stdout/stderr cross-stream interleaving non-determinism
- m80-uh6ex.3  document kernel-bump olddefconfig review requirement

#### Bundle F — File-size hard-limit refactor  [P1, ~1 PR]
CLAUDE.md: "Hard limit: 1000 lines — files crossing this are a bug to refactor before merging." launch.rs is 1281, with #1 churn rate.

- m80-ezs0x.1  launch.rs at 1281 lines
- m80-ezs0x.2  connection.rs (983) + lifecycle/exec.rs (892) approaching limit
- m80-ezs0x.3  extract assemble_running_sandbox helper

**Sequencing note:** this PR will conflict with Bundle C (launch failure-summary writes). Either land C first, then F refactors atop, or include the failure-summary plumb in the same refactor PR.

#### Bundle G — Identifier + path validation at construction  [P1+P2, ~1 PR]
9ihnr.1 (vm_id traversal) is the highest-severity new finding in the audit and the smallest fix. ag33f.4 (VmId/WorkspacePath newtypes) is the right shape.

- m80-9ihnr.1  vm_id has no charset/traversal validation (HIGH — path-traversal)
- m80-ag33f.4  VmId + workspace newtype with validation at construction
- m80-9ihnr.3  SandboxConfig.workspace accepts relative + .. paths
- m80-9ihnr.2  idle_timeout = Some(Duration::ZERO) busy-loops
- m80-9ihnr.9  JoinNetns: guest_mac (regex) + tap_name (IFNAMSIZ) validation
- m80-1vna7.3  M80_TEST_FAIL_* env hooks need cfg(test) guard
- m80-ag33f.5  SandboxConfig named consts + PartialEq + serde (partial; the named-consts slice)

### Tier 3 — Hygiene and follow-on cleanup

#### Bundle H — Test coverage backfill  [P1+P2, distributed across A–G + 1 orphan PR]
Per CLAUDE.md: "Touch a non-trivial invariant → write a regression test." Most of these should fold into the bundle that touches the same code; a single backfill PR catches the orphans.

- m80-vpw49.1  m80-firecracker — fold into Bundle B/C
- m80-vpw49.2  m80-jailer-harden caps + close_inherited_fds — standalone (security)
- m80-vpw49.3  m80-jailer Plan/recover — standalone (audit-eligible)
- m80-vpw49.4  m80-cgroup LivePids — standalone
- m80-vpw49.5  m80-snapshot corrupted bytes + .. traversal — fold into G
- m80-vpw49.6  m80-storage TemplateLock + ENOSPC + symlink — fold into J
- m80-vpw49.7  m80-vsock unbounded read_line — fold into Bundle B (close per-conn errors)
- m80-vpw49.8  m80-net-outbound IPv6/ICMP/ARP + collision rollback — fold into A
- m80-vpw49.9  m80-guestd 5 gaps — fold into C (panic hook)
- m80-vpw49.10 m80-image-* — fold into I
- m80-vpw49.11 m80-preflight + cli CVE fenceposts — standalone

#### Bundle I — Image-build correctness  [P2+P3, ~1 PR]

- m80-uh6ex.4  no sha256 verification on downloads
- m80-uh6ex.12 partial output + non-atomic ext4 on mid-build failure
- m80-uh6ex.5  guestd not chowned root:root
- m80-uh6ex.7  artifact dir symlinks followed
- m80-uh6ex.8  multi-kernel lex sort → reject ambiguity
- m80-uh6ex.9  root/CAP_SYS_ADMIN preflight
- m80-uh6ex.11 subprocess stderr capture uniformly
- m80-wok08.4  quickstart probe failure buries preflight error (same fix family)
- m80-73qb3.14 musl-static guestd validation
- m80-ag33f.8  CLI flag gaps — --scratch-size unit + --vcpu/--mem/--timeout + override precedence in --help

#### Bundle J — WarmPool small fixes + Drop discipline  [P2+P3, ~1 PR]
Follow-on hygiene after Bundle B.

- m80-r4308.13 verify RunningSandbox::Drop force-kills FC (investigation; gates the others)
- m80-r4308.8  fill_to_target_blocking doesn't increment filling
- m80-r4308.9  per-lease age tracking
- m80-ag33f.7  WarmPool docs + lease_with_timeout
- m80-243wj.13 WarmLease + WarmPool Drop swallow discard_sandbox errors
- m80-243wj.14 TemplateLock + LeaseGuard Drop swallow remove_file errors
- m80-mrjqs.6  vm_id correlation in tracing across Drop paths
- m80-243wj.4  stale TemplateLock + .tmp blocks next launches
- m80-243wj.19 TemplateLock should use flock instead of create_new spin

#### Bundle K — Misc smalls  [P2+P3, grab-bag PRs]
None are individually urgent; close opportunistically.

- m80-243wj.5  inspect_run_dir misclassify as OrphanJail
- m80-243wj.6  preflight memory.max ≥ mem_size_mib + FC_OVERHEAD
- m80-243wj.8  mid-launch EPERM capability hints
- m80-243wj.10 StoppedSandbox Drop impl
- m80-243wj.11 misc reliability (xtables -W, boot_identity, recover docs)
- m80-mrjqs.11 phase_1/phase_2 use diag_phase! macro
- m80-mrjqs.12 M80_GUEST_BOOT into diagnostics.jsonl
- m80-1vna7.1  memory_max hardcoded 1.5GiB doesn't track mem_size_mib
- m80-1vna7.2  --egress=outbound default is least-privilege inversion
- m80-1vna7.4  idle_timeout default(300s) vs CLI-always-None
- m80-1vna7.8  unexplained sleeps + duplicated EXEC_BUFFER_LIMIT constants
- m80-1vna7.9  Cargo workspace cleanups
- m80-73qb3.12 bridge route-collision check skipped on recovery
- m80-73qb3.13 ip_forward sysctl not restored on teardown
- m80-9ihnr.5  schema_version not checked on read (jailer + net-outbound; 3 sites)
- m80-9ihnr.8  clap declarative conflicts_with
- m80-vpifh.2  PROTOCOL_VERSION ≤ 255 static_assert
- m80-vpifh.3  known_envelope_field range drift check
- m80-vpifh.5  liveness misclassification of EBADF/ECONNRESET
- m80-ezs0x.4  demote test-seam pub items in m80-net-outbound
- m80-ezs0x.7  single TODO ref
- m80-ag33f.6  RunningSandbox docs (failed-exec recovery, ForceKillGuard, SO_RCVTIMEO)
- m80-ag33f.9  ExecRequest ergonomics
- m80-wok08.10 JSON output gaps (Vec<u8>, inner version, bare json!{})
- m80-wok08.11 preflight hint quality
- m80-kv4ri.3  launch_from_snapshot discards Discovery (let _ =)
- m80-kv4ri.13 snapshot_restore REST timeout
- m80-73qb3.3  AMD T2A CpuTemplate (only if AMD deployment is real)

---

## Ranked sequence

Recommended order, optimizing for unblocking torpor2 first and CLAUDE.md compliance second:

1. **Bundle A**  — network cleanup correctness  (real silent host pollution today)
2. **Bundle B**  — warm owner reliability  (SIGTERM corrupts pool today)
3. **Bundle C**  — failure post-mortem visibility  (operator can't debug today)
4. **Bundle E**  — README drift batch  (cheap; high closeout ratio; do alongside above)
5. **Bundle D**  — FcError surface hygiene  (torpor2 consumer ergonomics; ag33f.2 unblocks recovery decisions)
6. **Bundle G**  — identifier validation  (9ihnr.1 path-traversal is the highest new finding)
7. **Bundle F**  — file-size limit refactor  (CLAUDE.md hard rule; coordinate with C)
8. **Bundle J**  — WarmPool follow-on hygiene
9. **Bundle I**  — image-build correctness + quickstart UX
10. **Bundle H**  — test backfill orphans
11. **Bundle K**  — misc smalls (grab-bag PRs as time permits)

**Total expected closures from Tier 1+2 bundles (A–G):** ~70 beads in ~5–7 focused PRs.
**Total expected closures from Tier 3 (H–K):** ~26 beads in 4–6 cleanup PRs.
**Closed-as-dup (immediate):** 7 beads.
**Deferred (no current consumer signal):** 62 beads remain in backlog with `defer` tag.

Approximate active worklist after triage: **96 beads, 11 bundles, 9–13 PRs.**
